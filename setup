#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
TARGET_REPO="${1:-$PWD}"

cd "$SCRIPT_DIR"
python3 -m pip install -e .
npx skills add ./src/skills --all -y
python3 -m clawdibrate --repo "$TARGET_REPO" --setup
