# clwdi-v0_13_0-003 — Configure uv, mypy, ruff, VS Code workspace

**Status:** done
**Priority:** high
**Complexity:** S

## What

Set up dev tooling: uv for package management, mypy for type checking, ruff for linting/formatting, VS Code workspace settings.

## Changes

1. `pyproject.toml` — added `[project.optional-dependencies] dev`, `[tool.mypy]`, `[tool.ruff]`
2. `.vscode/settings.json` — Pylance autocomplete only, mypy squiggles, ruff format-on-save
3. `AGENTS.md` — added `uv` as package manager note in Setup
4. `docs/development.md` — uv-based setup instructions

## Acceptance

- `uv run mypy clawdibrate/` clean
- `uv run ruff check clawdibrate/` reports only pre-existing long lines in orchestrator.py
- VS Code shows mypy + ruff squiggles
