---
id: "003"
title: MCP Resources — sessions, git-history, transcripts, scores
status: todo
priority: high
depends_on: ["002"]
---

# MCP Resources — sessions, git-history, transcripts, scores

Expose evidence as read-only MCP resources, replacing the recording skills.

## Context

The prompt-only recording skills (`record-start`/`record-stop`/`record-from-git`) are retired. The
server surfaces sessions and git-derived transcripts directly, backed by existing `session_dump.py`
and `git_history.py`. See SPEC.md Resources + decision #5.

## Acceptance Criteria (locked URIs)

- [ ] `clawdibrate://sessions` — list available Claude Code sessions (`session_dump.py`)
- [ ] `clawdibrate://sessions/{id}` — one session as a transcript
- [ ] `clawdibrate://git-history` — synthesized transcript from git (`git_history.py`)
- [ ] `clawdibrate://transcripts/{name}` — existing `.clawdibrate/transcripts/*.jsonl`
- [ ] `clawdibrate://history/scores` — `.clawdibrate/history/scores.jsonl` (former `--scores`)
- [ ] All resources read-only; no model calls
- [ ] Reuse existing modules; no duplicated parsing logic

## Notes

Replaces card-retired recording skills (see card 009). Part of v1.0.0. See SPEC.md.
