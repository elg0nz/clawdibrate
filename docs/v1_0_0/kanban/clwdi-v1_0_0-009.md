---
id: "009"
title: README rewrite + version bump to 1.0.0
status: todo
priority: high
depends_on: ["001", "002", "004", "008"]
---

# README rewrite + version bump to 1.0.0

Rewrite README for the MCP architecture and do the atomic MAJOR bump to 1.0.0.

## Context

Current README documents the old `pip` / `python -m clawdibrate` flow. v1.0.0 is MCP-first with a
downscoped CLI. MAJOR is human-approved. Bump is atomic across pyproject + AGENTS.md header +
CHANGELOG (per Boundaries). LICENSE (MIT) already added. See SPEC.md.

## Acceptance Criteria

- [ ] README leads with the AGENTS.md Scorecard + the two-layer (MCP / downscoped CLI) model
- [ ] Honest install: `uvx clawdibrate-mcp` + `.mcp.json` snippet (matches what ships)
- [ ] Explains the three MCP primitives (resources/tools/prompts)
- [ ] Removes stale `pip install git+` / `python -m clawdibrate` instructions
- [ ] Keeps the sqlite-utils before/after proof + arXiv citation
- [ ] `pyproject.toml` 0.14.0 → 1.0.0; AGENTS.md header → 1.0.0; CHANGELOG entry
- [ ] Single atomic version-bump commit; LICENSE referenced

## Notes

Near-end card. Depends on the real interfaces existing. Part of v1.0.0. See SPEC.md.
