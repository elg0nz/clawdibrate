---
id: "002"
title: Decide packaging + entrypoint (uvx / PyPI) and reconcile with uv-only rule
status: todo
priority: high
depends_on: []
---

# Decide packaging + entrypoint (uvx / PyPI) and reconcile with uv-only rule

Resolve how the MCP server is installed and invoked. This is a decision card — output is a
documented choice in SPEC.md, not code.

## Context

The post's above-the-fold command is `uvx clawdibrate-mcp`. Today's README uses
`pip install git+…` + `python -m clawdibrate`, which contradicts both uvx and the AGENTS.md
"uv only — never pip install" rule. Must pick one and make it real.

## Acceptance Criteria

- [ ] Choose: publish to PyPI (`uvx clawdibrate-mcp`) vs `uvx --from git+…` vs `python -m clawdibrate --mcp`
- [ ] Choose entrypoint name: `clawdibrate-mcp` / `clawdibrate mcp` / module flag
- [ ] Decision recorded in SPEC.md Open Decisions
- [ ] If PyPI: add a "publish to PyPI" release task
- [ ] Chosen command matches the post's above-the-fold install exactly (or post is updated)

## Notes

Blocks 001. Press-release-driven: whatever we pick becomes the post's claim. See SPEC.md.
