---
id: "001"
title: MCP server scaffold - clawdibrate-mcp exposing 3 tools
status: todo
priority: high
depends_on: ["002", "003"]
---

# MCP server scaffold - clawdibrate-mcp exposing 3 tools

Stand up the self-hosted MCP server that wraps the existing calibration engine and
exposes `score_agents_md`, `diagnose_from_transcript`, and `calibrate_agents_md`.

## Context

Today the engine ships only as the `/clawdbrt:loop` skill (claude-code/cursor/codex via
`npx skills add`). v1.0.0 promotes it to an MCP server so any MCP-native agent can call it.
Reuse the existing Python (`clawdibrate/` modules), per the "uv only" rule. See SPEC.md.

## Acceptance Criteria

- [ ] MCP server process starts and is reachable via `.mcp.json` stdio config
- [ ] Registers exactly 3 tools: `score_agents_md`, `diagnose_from_transcript`, `calibrate_agents_md`
- [ ] `diagnose_from_transcript` wraps bug-identifier + judge prompts
- [ ] `calibrate_agents_md` wraps the existing orchestrator loop
- [ ] No new copy of engine logic — server calls existing `clawdibrate/` modules
- [ ] Existing `/clawdbrt:loop` skill still works (back-compat)

## Notes

Depends on packaging (002) and the model-dependency decision (003). Part of v1.0.0. See SPEC.md.
