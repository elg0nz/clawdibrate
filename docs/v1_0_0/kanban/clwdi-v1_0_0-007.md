---
id: "007"
title: Retire the four bundled skills + CLI/settings cleanup
status: todo
priority: medium
depends_on: ["003", "004"]
---

# Retire the four bundled skills + CLI/settings cleanup

Remove the bundled skills now that resources + tools + prompts cover their behavior, and delete the
dead `--setup` machinery.

## Context

`clawdibrate/skills/{record-start,record-stop,record-from-git,loop}` are superseded: recording →
resources (003), loop → tools (004). `--setup`'s skills-install and settings.json permission
injection are old-architecture artifacts. See SPEC.md decision #2 and "What the CLI downscope means".

## Acceptance Criteria

- [ ] Remove `clawdibrate/skills/record-start`, `record-stop`, `record-from-git`, `loop`
- [ ] Remove `_install_bundled_skills()` and `_bundled_skills_path()`
- [ ] Remove `_ensure_permissions()` + `CLAWDIBRATE_PERMISSIONS` + settings.json write
- [ ] Drop `skills/*/SKILL.md` from package-data if no longer shipped
- [ ] Confirm `init-pointer` (CLI) covers the only surviving `--setup` job
- [ ] Tests for removed paths deleted/updated

## Notes

Depends on resources (003) + tools (004) existing first. Part of v1.0.0. See SPEC.md.
