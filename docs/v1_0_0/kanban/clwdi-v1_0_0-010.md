---
id: "010"
title: AGENTS.md one-liner convention - update for MCP distribution
status: todo
priority: low
depends_on: ["002", "006"]
---

# AGENTS.md one-liner convention - update for MCP distribution

Update the "add this line to any repo's AGENTS.md" onboarding snippet so it points at the MCP
install path instead of the old `pip install git+…` / `python -m clawdibrate` flow.

## Context

The README's distribution mechanism is a one-liner users paste into a target repo's AGENTS.md.
It currently advertises the pip/CLI install. After v1.0.0 it should advertise the MCP server.
Note: this snippet must NOT be added to clawdibrate's own AGENTS.md (per its header note).

## Acceptance Criteria

- [ ] Snippet updated to the card-002 install/entrypoint
- [ ] `docs/setup-convention.md` updated to match
- [ ] Confirms the snippet is for target repos, not clawdibrate's own AGENTS.md
- [ ] No fabricated commands — matches what actually ships

## Notes

Low priority, but closes the distribution loop. Part of v1.0.0. See SPEC.md.
