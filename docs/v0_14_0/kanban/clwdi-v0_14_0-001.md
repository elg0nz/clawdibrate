# clwdi-v0_14_0-001 — Extract metrics.py from orchestrator

**Status:** done
**Priority:** high
**Complexity:** M

## What

Extract all deterministic metric functions into `clawdibrate/metrics.py`.

## Changes

1. `clawdibrate/metrics.py` — `compute_metrics()`, `rouge_l_similarity()`, `split_transcripts()`, `compute_edit_distance()`, `compute_recency_weight()`, `compute_diversity()`
2. `tests/test_metrics.py` — 18 tests covering all extracted functions
3. `clawdibrate/orchestrator.py` — removed definitions, added imports

## Acceptance

- All metric tests pass
- `from clawdibrate.orchestrator import compute_metrics` still works (re-export)
