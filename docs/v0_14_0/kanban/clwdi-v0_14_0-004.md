# clwdi-v0_14_0-004 — Extract history.py from orchestrator

**Status:** done
**Priority:** high
**Complexity:** S

## What

Extract persistence and convergence tracking into `clawdibrate/history.py`.

## Changes

1. `clawdibrate/history.py` — `load_reflections()`, `save_reflection()`, `save_score()`, `save_instrumentation()`, `estimate_iterations_to_target()`, `load_baselines()`, `save_baseline()`, `is_converged()`, `central_scoreboard_path()`
2. `tests/test_history.py` — 8 tests

## Acceptance

- History tests pass
- Score persistence and convergence detection identical to original
