# clwdi-v0_14_0-002 — Extract agent_execution.py from orchestrator

**Status:** done
**Priority:** high
**Complexity:** S

## What

Extract agent CLI invocation logic into `clawdibrate/agent_execution.py`.

## Changes

1. `clawdibrate/agent_execution.py` — `AGENT_COMMANDS`, `resolve_default_calibration_agent()`, `apply_builtin_model_flag()`, `run_agent()`, `extract_json()`, model helpers
2. `tests/test_agent_execution.py` — 8 tests
3. `clawdibrate/orchestrator.py` — removed definitions, added re-exports for `ralph.py` compatibility

## Acceptance

- Agent execution tests pass
- `ralph.py` imports from orchestrator still resolve
