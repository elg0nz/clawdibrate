# clwdi-v0_14_0-003 — Extract repo.py from orchestrator

**Status:** done
**Priority:** high
**Complexity:** M

## What

Extract instruction file operations into `clawdibrate/repo.py`.

## Changes

1. `clawdibrate/repo.py` — `resolve_repo_root()`, `repo_paths()`, `read_instruction_file()`, `parse_instruction_version()`, `bump_patch_version()`, `snapshot_iteration_file()`, `extract_section()`, `replace_section()`, `strip_prompt_artifacts()`, `validate_no_prompt_leaks()`, git helpers
2. `tests/test_repo.py` — 11 tests

## Acceptance

- Repo tests pass
- Section extract/replace works identically to original
