# clwdi-v0_12_0-001 — Bootstrap mode: grow AGENTS.md from failures when no sections exist

**Status:** done
**Priority:** high
**Complexity:** M

## What

When `responsible_section` names a section that doesn't exist in AGENTS.md, route those failures through a bootstrap path that creates the section rather than dropping it.

## Changes

1. `clawdibrate/prompts/bug-identifier.md` — propose meaningful section name instead of `"unknown"` ✅ (done in session)
2. `clawdibrate/orchestrator.py` — detect empty-section failures in `_run_stage_implement`, build bootstrap prompt, append new section to AGENTS.md
3. Print `[bootstrap] creating new section '...'` to stdout so the user can see it happening

## Acceptance

- 1-line AGENTS.md + existing transcripts → at least one new `## Section` appended after calibration
- Existing edit-section path unaffected
