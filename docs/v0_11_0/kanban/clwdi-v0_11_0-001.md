# clwdi-v0_11_0-001: Make progressive the default loop mode

**Status:** done
**Priority:** P0
**Size:** S
**Wave:** 1
**Depends on:** none

## Description
Change the default `--mode` from `standard` to `progressive`. 90% of users want cancel-safe iterative calibration — progressive should be the zero-config default.

Files changed:
- `clawdibrate/__main__.py` — argparse default
- `clawdibrate/orchestrator.py` — `calibrate()` function signature default

## Acceptance
- `python -m clawdibrate --help` shows `default: progressive`
- Running `python -m clawdibrate` without `--mode` enters progressive mode
