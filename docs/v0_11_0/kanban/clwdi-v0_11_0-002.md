# clwdi-v0_11_0-002: Remove legacy `standard` mode

**Status:** done
**Priority:** P1
**Size:** S
**Wave:** 1
**Depends on:** clwdi-v0_11_0-001

## Description
Kill the `standard` mode entirely — all users are on latest. It was a single-pass with no opinionated defaults, effectively a less useful `fast`.

Files changed:
- `clawdibrate/__main__.py` — removed from choices
- `clawdibrate/orchestrator.py` — default param updated
- `tests/test_calibrate.py` — two assertions updated from `standard` to `progressive`
- `docs/v0_10_0/SMOKE_TEST.md` — removed standard smoke test section

## Acceptance
- `--mode standard` is rejected by argparse
- No references to `"standard"` in `.py` or `.md` files
