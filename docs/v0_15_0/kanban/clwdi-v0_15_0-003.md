# clwdi-v0_15_0-003: CLI and reporting

**Status:** todo
**Priority:** P1
**Depends on:** clwdi-v0_15_0-002

Wire up `clawdibrate enforce` CLI command and violation reporting.

## Tasks
- [ ] Add `enforce` subcommand to CLI
- [ ] `rules/report.py` — terminal output (file:line + violated rule), JSON output, exit code
- [ ] `--staged` flag to scope to git-staged files only
- [ ] `--format=json` for CI integration
- [ ] `--rules` flag to specify instruction file path (default: auto-detect)
