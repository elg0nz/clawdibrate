# clwdi-v0_15_0-001: Rule parser and registry

**Status:** todo
**Priority:** P0

Parse CLAUDE.md / AGENTS.md into typed, checkable rule objects.

## Tasks
- [ ] `rules/parser.py` — read instruction file, identify enforceable blocks using signal words (MANDATORY, MUST, NEVER, ALWAYS)
- [ ] `rules/registry.py` — `Rule` base class with `check(codebase) -> list[Violation]` interface
- [ ] Rule type classification (component-size, no-banner-comments, required-library, file-pattern, forbidden-pattern)
- [ ] Tests for parser extraction from real CLAUDE.md examples
