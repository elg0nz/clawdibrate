# clwdi-v0_10_0-009: Extract `_run_stage_judge()`

**Status:** done
**Priority:** P0
**Size:** S
**Wave:** 2
**Depends on:** 001

## Description
Extract lines ~1164-1206 into `_run_stage_judge(all_pending_failures, agents_md, workers, model, agent) -> tuple[list[dict], dict[str, list[float]]]`. Returns `(all_failures, section_scores)`.

## Acceptance
- All existing tests still pass
- Judge error/low-weight tests still verify correct behavior
