# clwdi-v0_10_0-013: `--scores --chart` ASCII sparkline

**Status:** todo
**Priority:** P2
**Size:** S
**Wave:** 1
**Depends on:** none

## Description
Add `--scores` flag to `__main__.py` that reads `scores.jsonl` and prints:
- Table of last 10 scores with timestamp, avg, section breakdown
- ASCII sparkline of avg score trend (e.g. `Score: ▁▂▃▅▆▇█▇█▇ 0.87`)

No external dependencies — use Unicode block chars.

## Acceptance
- `python -m clawdibrate --scores` prints readable score history
- Works with 0 scores (prints "no scores yet")
