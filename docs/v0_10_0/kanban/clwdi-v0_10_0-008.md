# clwdi-v0_10_0-008: Extract `_run_stage_bug_id()`

**Status:** todo
**Priority:** P0
**Size:** S
**Wave:** 2
**Depends on:** 001

## Description
Extract lines ~1111-1162 into `_run_stage_bug_id(transcript_data, agents_md, instruction_path, workers, model, agent) -> list[tuple[dict, dict]]`. Returns `all_pending_failures` (failure, transcript_data pairs).

## Acceptance
- All existing tests still pass
- Stage 1 tests still verify sequential vs parallel paths
