# clwdi-v0_10_0-006: Extract `_discover_transcripts()`

**Status:** todo
**Priority:** P0
**Size:** S
**Wave:** 2
**Depends on:** 001

## Description
Extract lines ~1023-1057 of `calibrate()` into `_discover_transcripts(transcript_path, repo_root, transcripts_dir, max_transcripts, holdout_ratio) -> tuple[list[Path], list[Path]]`. Returns `(train_transcripts, test_transcripts)`.

## Acceptance
- All existing tests still pass
- `radon cc` shows `calibrate` complexity reduced
