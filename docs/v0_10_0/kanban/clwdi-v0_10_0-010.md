# clwdi-v0_10_0-010: Extract `_run_stage_impl()`

**Status:** todo
**Priority:** P0
**Size:** S
**Wave:** 2
**Depends on:** 001

## Description
Extract lines ~1215-1349 into `_run_stage_impl(all_failures, agents_md, reflections, tokens_start, hard_token_cap, workers, model, agent) -> tuple[str, dict[str, int], dict[str, dict]]`. Returns `(updated_agents_md, edit_distances, diversity_metrics)`.

## Acceptance
- All existing tests still pass
- Implementer tests (empty, leak, budget, success) all still pass
